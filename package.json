{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev:host": "export NODE_TLS_REJECT_UNAUTHORIZED=0 && nuxt --env.NODE_TLS_REJECT_UNAUTHORIZED=0 && nuxt dev --hostname 'wajitech.fr' --port 3002",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "@types/node": "^17.0.30",
    "nuxt": "3.0.0-rc.1"
  },
  "dependencies": {
    "bootstrap": "^5.1.3",
    "firebase": "^9.7.0",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "unenv": "^0.5.1"
  }
}
